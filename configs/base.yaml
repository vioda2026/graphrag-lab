runtime:
  mode: local-debug
  seed: 7
  output_dir: artifacts

builder:
  min_edge_weight: 0.05

explorer:
  top_k: 3

retriever:
  top_k: 3

reader:
  mode: extractive

data:
  toy_data_path: data/toy/dataset.yaml
